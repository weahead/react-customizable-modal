{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bs": "yarn",
    "start": "cd ./packages/react-customizable-modal/ && yarn run build-watch",
    "build": "cd ./packages/react-customizable-modal/ && yarn run build"
  }
}
